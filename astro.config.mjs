import { defineConfig } from 'astro/config';

// Site déployé en statique (Vercel / Netlify gratuit) — voir README.md
export default defineConfig({
  site: 'https://gino-fish-and-cook.vercel.app',
});
